# /app/javascript/components/AddPeerSetForm/operations.graphql

mutation AddPeerSetMutation(
  $requesteePeerEmail: String!
) {
  addPeerSet(requesteePeerEmail: $requesteePeerEmail!) {
    peerSet
  }
}
